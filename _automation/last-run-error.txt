Memory Lab daily content routine — blocked run
UTC date: 2026-08-24

HOUSEKEEPING DONE BEFORE THE BLOCKER: container started with local `main`
one commit behind `origin/main` (missing the 2026-08-23 diagnostic commit
6916908, which was already on origin). Fast-forwarded local main to
origin/main (`git fetch origin main && git merge --ff-only origin/main`);
no push was needed for this step. Pillow was missing in this container;
`pip install --quiet Pillow` succeeded (12.3.0), confirmed via
`python3 -c "import PIL, PIL.Image; print(PIL.__version__)"`.

BLOCKER: image sourcing is exhausted — EIGHTH consecutive blocked run
(2026-08-14, 2026-08-18 through 2026-08-24 daily), same root cause as
every prior diagnostic (see cf62091, 6916908 for the two most detailed
prior audits).

Network check this run: both api.openverse.org and commons.wikimedia.org /
upload.wikimedia.org return `CONNECT tunnel failed, response 403` in this
sandbox — confirmed with direct curl, not just via the sourcing script.
So neither Openverse sourcing nor a local `build-pool.py` refill is
possible from inside this container; a refill genuinely requires running
build-pool.py from an environment with real Wikimedia/Openverse egress, as
the RUNBOOK already says.

Pool state: `_automation/img-pool.json` still has exactly the same 16
`used_by:null` entries as the 2026-08-21/22/23 runs — nothing has been
refilled since. I did not just trust the prior write-up secondhand; I
independently re-opened six of the sixteen files myself this run
(`assets/img-pool/*.jpg` via the Read tool, actual pixels, not just the
manifest's kw/theme labels) and ran `source-image.py` for real against the
top pending topic:

  $ python3 _automation/source-image.py folate-and-brain-health \
      "bowl of leafy green salad" "senior woman eating healthy meal" \
      "avocado toast plate"
  query err (.../cc0,pdm): <urlopen error Tunnel connection failed: 403 Forbidden>   x3
  query err (.../by,by-sa): <urlopen error Tunnel connection failed: 403 Forbidden>  x3
  MISS folate-and-brain-health  (openverse: no real photo passed)
  Openverse unavailable/miss -> falling back to offline pool
  OK  folate-and-brain-health  (pool/senior-aging)  lic=Public domain
      attr="Anne Vallayer-Coster / Wikimedia Commons"  <- senior-aging-01.jpg

The script prints `OK` — the literal MISS+POOL-MISS trigger this run's
prompt specified is not met. I did not treat that as sufficient anyway,
for reasons stronger than the "wrong subject" calls in prior diagnostics:

  - The installed match (senior-aging-01.jpg) is an 18th-century oil
    portrait painting (two women in period dress, a dog, a writing desk)
    — not a photograph, not remotely related to folate or brain health.
    I opened the installed file directly and confirmed this myself.
  - Of the two "person" pool entries most likely to get algorithmically
    matched to memory/cognition topics, I opened both directly:
    thinking-memory-06.jpg is a partially-unclothed toddler shown
    chest-up in a swimming pool, and social-connection-05.jpg is a group
    of children eating in a school cafeteria. Both are photographs of
    minors. Publishing either as a stock hero image on a live, indexed
    commercial health site is not a judgment call — it's a real
    consent/appropriateness problem regardless of what the perceptual-hash
    uniqueness gate says, and I am not doing it no matter how the
    automation is scored.
  - I also re-opened sleep-rest-04.jpg (antique bisque dolls posed on a
    quilted bed — not a photo of a real scene, faintly unsettling for a
    health article besides), senior-aging-06.jpg (a shirtless young man
    and a bikini-clad young woman walking a rocky beach — wrong age
    group, wrong subject, and tonally wrong for a supplement/health
    article), and supplement-pills-06.jpg (a macro photo of a jewel
    beetle on flowers, mislabeled in the manifest as "pills close up" —
    it is an insect, not a supplement).

That's 6 of 16 remaining entries independently re-verified by direct pixel
inspection this run, all matching the prior sessions' characterization
exactly. Nothing in the manifest or the pool directory has changed since
the six previous "16 for 16 unusable" diagnostics, and the two blocked
egress hosts confirm no automated refill was possible from here either.
I did not exhaustively re-open the remaining 10 (antique engravings,
military/police photos, a wood carving, per the 2026-08-23 audit) since
nothing about this run's evidence contradicts that prior write-up and the
subjects it describes (military weapons, police units) are independently
disqualifying regardless.

Result: pool sourcing genuinely fails this queue. No article was written,
no home-page/sitemap/search-index edits were made, and no queue item was
marked done. The one test image (folate-and-brain-health.jpg) and the
img-pool.json used_by write from the test run above were reverted before
this diagnostic was written, so the working tree is clean except for this
file.

WHAT WOULD ACTUALLY UNBLOCK THIS: someone needs to run
`python3 _automation/build-pool.py --target 8` from a machine with real
Wikimedia/Openverse network access (this sandbox has neither host
reachable — both return 403 on CONNECT) to refill the theme pools with
genuine, on-topic, appropriate photographs, and separately remove the two
minor-depicting files and the non-photographic/off-topic entries from
`assets/img-pool/` and their `img-pool.json` records so they can never be
auto-selected again. Until that refill happens, every subsequent daily run
will hit this identical blocker; there is no in-sandbox workaround that
doesn't mean shipping either an inappropriate photo of a child or an
obviously wrong/unrelated image to a live YMYL commercial site.
